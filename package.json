{
  "name": "@adp/backstage",
  "version": "0.1.0",
  "private": true,
  "description": "Backstage plugins for the Agent Developer Portal (ADP)",
  "engines": {
    "node": "20 || 22"
  },
  "scripts": {
    "tsc": "tsc",
    "build": "backstage-cli repo build --all",
    "lint": "backstage-cli repo lint",
    "test": "backstage-cli repo test",
    "clean": "backstage-cli repo clean"
  },
  "workspaces": {
    "packages": [
      "plugins/*"
    ]
  },
  "devDependencies": {
    "@backstage/cli": "^0.28.0",
    "typescript": "~5.4.0"
  },
  "packageManager": "yarn@4.4.1",
  "license": "Apache-2.0"
}
